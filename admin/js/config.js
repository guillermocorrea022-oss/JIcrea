// Ruta base de la API PHP. Si la web y el /admin están en el mismo dominio
// (ej. jicrea.com/admin y jicrea.com/api), dejá '/api'. No hace falta tocar
// nada más: las credenciales de la base viven en el servidor (api/config.php).
export const API_BASE = '/api';
